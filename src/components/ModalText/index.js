import React from 'react';
import { Text } from './modalText-styles';

const ModalText = ( {text} ) => {
    return (
        <Text>
            {text}
        </Text>
    )
};

export default ModalText;